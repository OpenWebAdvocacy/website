{% extends "layouts/base.njk" %}

{% block content %}
  <article class="h-entry [ post ] [ flow ]" {% if translated === false %} lang="en" {% endif %}>
    <h1 class="p-name">{{ title }}</h1>

    {% if translated === false %}
      {% include "partials/banner-translation.njk" %}
    {% endif %}

    {{ content | safe }}
  </article>
{% endblock %}
